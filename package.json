{
  "name": "mailor",
  "version": "0.1.3",
  "description": "Mailer tools",
  "main": "lib/mailer.js",
  "bin": {
    "mailor": "bin/cli"
  },
  "scripts": {
    "build": "mkdir -p dist && cp node_modules/somedom/dist/somedom.umd.js dist/",
    "lint": "eslint bin lib tests public",
    "pretest": "npm run lint",
    "start": "mailor watch bin/example/templates -w bin/example/partials -j bin/example/defaults.json -d dist/generated",
    "mocha": "NODE_ENV=test _mocha --timeout 5000 --exit --recursive -bR spec",
    "test": "npm run test:unit --",
    "test:ci": "npm run coverage && npm run report -- -r lcov",
    "test:unit": "npm run mocha -- 'tests/*.test.js'",
    "test:coverage": "npm run coverage && npm run report",
    "coverage": "nyc --silent -x '**/tests/**' -x '**/*.test.js' -- npm run test:unit",
    "codecov": "codecov --file=coverage/lcov.info -e TRAVIS_NODE_VERSION",
    "report": "nyc report"
  },
  "files": [
    "bin/*",
    "lib/*.js",
    "dist/*.js",
    "public/*.*",
    "provider.js"
  ],
  "keywords": [
    "nodemailer",
    "templates",
    "preview",
    "maildev",
    "mailer"
  ],
  "author": "Alvaro Cabrera <pateketrueke@gmail.com> (https://soypache.co)",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^9.0.1",
    "glob": "^7.1.6",
    "live-server": "^1.2.1",
    "maildev": "^1.1.0",
    "mjml": "^4.6.3",
    "mustache": "^4.0.1",
    "nodemailer": "^6.0.0",
    "pug": "^3.0.0",
    "wargs": "^0.9.2"
  },
  "devDependencies": {
    "axios": ">=0.18.1",
    "chai": "^4.2.0",
    "codecov": "^3.6.1",
    "eslint": "^7.10.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.0",
    "kind-of": ">=6.0.3",
    "minimist": ">=1.2.3",
    "mocha": "^7.2.0",
    "mock-stdin": "^1.0.0",
    "nyc": "^15.1.0",
    "open": ">=6.0.0",
    "somedom": "^0.1.1",
    "std-mocks": "^1.0.1",
    "tempfile": "^3.0.0",
    "testdouble": "^3.12.4"
  },
  "optionalDependencies": {
    "nsfw": "^2.0.0"
  }
}
